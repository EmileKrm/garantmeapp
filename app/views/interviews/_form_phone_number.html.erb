<div class="embedded-form">
  <%= simple_form_for user, remote: true do |f| %>
    <%= f.input :phone_number, label: false, placeholder:'(+33) 06 00 00 00 00' %>
    <%= f.submit 'Save', class: "btn btn-primary save-step" %>
  <% end %>
</div>
